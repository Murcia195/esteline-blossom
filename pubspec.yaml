name: tienda_maquillaje
description: Aplicación de Tienda de Maquillaje con Supabase
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.10.3
  flutter_dotenv: ^6.0.0
  font_awesome_flutter: ^10.4.0
  google_sign_in: ^6.2.0
  url_launcher: ^6.3.0
  shared_preferences: ^2.2.0
  flutter_local_notifications: ^17.0.0
  awesome_notifications: ^0.8.2
  image_picker: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - .env
    - imagenes/LogoEsteline.jpg
